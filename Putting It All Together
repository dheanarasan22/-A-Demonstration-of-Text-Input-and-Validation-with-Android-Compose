@Composable
fun MainScreen() {
    Scaffold(
        topBar = { TopAppBar(title = { Text("Compose Input Demo") }) }
    ) {
        LoginScreen()
    }
}

@Preview(showBackground = true)
@Composable
fun MainScreenPreview() {
    MainScreen()
}
